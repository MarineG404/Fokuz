// Son manager migré vers expo-audio avec hooks React
// Utilise le contexte AudioContext pour accéder aux sons

import { soundManager } from "@/contexts/AudioContext";

// Ré-export du sound manager depuis le contexte
export { soundManager };
